import type {ReactNode} from "react";
export function SectionHeading({eyebrow,title,subtitle,align="left"}:{eyebrow:string;title:string;subtitle?:string;align?:"left"|"center"}){return <div className={`mb-12 max-w-2xl ${align==="center"?"mx-auto text-center":""}`}><p className={`eyebrow ${align==="center"?"eyebrow-center":""}`}>{eyebrow}</p><h2 className="h-serif mt-4 text-3xl leading-tight sm:text-4xl">{title}</h2>{subtitle&&<p className="body-lead mt-4">{subtitle}</p>}</div>}
export function Section({children,className="",id}:{children:ReactNode;className?:string;id?:string}){return <section id={id} className={`py-20 sm:py-28 ${className}`}><div className="container-site">{children}</div></section>}
