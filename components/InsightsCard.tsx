import Link from "next/link";
import {ArrowUpRight,PencilLine} from "lucide-react";
export type Insight={slug:string;title:string;excerpt:string;tag:string;draft:boolean};
export function InsightsCard({article,locale,readMore,cmsNote}:{article:Insight;locale:string;readMore:string;cmsNote:string}){return <article className="accent-line card-base group flex flex-col p-8 transition-all duration-300 hover:-translate-y-[2px] hover:border-navy/25 hover:shadow-lift"><div className="flex items-center gap-3"><span className="rounded-full bg-navy-50 px-3 py-1 text-[11px] font-semibold uppercase tracking-[.14em] text-navy">{article.tag}</span>{article.draft&&<span className="inline-flex items-center gap-1 rounded-full border border-fred/30 bg-white px-2.5 py-1 text-[10px] font-semibold uppercase tracking-[.12em] text-fred" title={cmsNote}><PencilLine className="h-3 w-3"/>CMS</span>}</div><h3 className="mt-5 font-serif text-xl font-medium leading-snug text-ink">{article.title}</h3><p className="mt-3 text-sm leading-relaxed text-muted">{article.excerpt}</p><Link href={`/${locale}/insights#${article.slug}`} className="focus-ring mt-auto inline-flex items-center gap-1.5 pt-6 text-sm font-semibold text-navy underline-offset-4 hover:underline">{readMore}<ArrowUpRight className="h-4 w-4 transition-transform group-hover:translate-x-.5 group-hover:-translate-y-.5"/></Link></article>}
