"use client";
import {motion,useReducedMotion} from "framer-motion";
type Step={title:string;text:string};
export function ProcessTimeline({steps}:{steps:Step[]}){const reduce=useReducedMotion();return <ol className="relative grid gap-10 md:grid-cols-4 md:gap-6"><div aria-hidden="true" className="absolute left-[15px] top-2 h-[calc(100%-2rem)] w-px bg-line md:left-0 md:top-[15px] md:h-px md:w-full"/>{steps.map((step,i)=><motion.li key={step.title} className="relative flex gap-6 md:block" initial={reduce?false:{opacity:0,y:14}} whileInView={{opacity:1,y:0}} viewport={{once:true,margin:"-60px"}} transition={{duration:.5,delay:i*.12,ease:[.22,1,.36,1] as [number,number,number,number]}}><span className="relative z-10 flex h-8 w-8 shrink-0 items-center justify-center rounded-full border border-navy/25 bg-white font-serif text-sm font-semibold text-navy">{i+1}</span><div className="md:mt-5"><h3 className="font-serif text-lg font-medium text-ink">{step.title}</h3><p className="mt-2 text-sm leading-relaxed text-muted">{step.text}</p></div></motion.li>)}</ol>}
