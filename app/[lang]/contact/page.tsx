import {Phone} from "lucide-react";
import {getDictionary} from "@/lib/i18n/get-dictionary";
import {Section} from "@/components/SectionHeading";
import {WhatsAppButton,CallButton} from "@/components/ContactActions";
import {ContactForm} from "@/components/ContactForm";
import {LegalDisclaimer} from "@/components/LegalDisclaimer";
import {site} from "@/lib/site";
export default function ContactPage({params:{lang}}:{params:{lang:"fr"|"en"}}){const dict=getDictionary(lang);return <><Section className="relative overflow-hidden bg-gradient-to-b from-parchment via-white to-white text-center"><p className="eyebrow">{dict.contact.eyebrow}</p><h1 className="h-serif mx-auto mt-4 max-w-3xl text-4xl leading-tight sm:text-5xl">{dict.contact.pageTitle}</h1><p className="body-lead mx-auto mt-6 max-w-2xl">{dict.contact.subtitle}</p><div className="mt-10 flex flex-wrap items-center justify-center gap-4"><WhatsAppButton label={dict.actions.startWhatsapp} message={dict.actions.whatsappMessage} large/><CallButton label={dict.actions.callLabel} large/></div><p className="mt-8 flex items-center justify-center gap-2 text-sm text-muted"><Phone className="h-4 w-4 text-navy"/><a href={site.phoneHref} className="focus-ring rounded-sm font-semibold text-navy underline-offset-4 hover:underline">{site.phoneDisplay}</a></p><p className="mt-3 text-xs uppercase tracking-[.18em] text-muted">{dict.contact.localNote}</p></Section><Section className="!pt-4"><div className="mx-auto max-w-3xl"><h2 className="font-serif text-2xl font-medium text-ink sm:text-3xl">{dict.contact.formTitle}</h2><p className="mt-3 text-sm leading-relaxed text-muted">{dict.contact.formIntro}</p><div className="mt-8"><ContactForm dict={dict}/></div><LegalDisclaimer dict={dict} className="mt-8"/></div></Section></>}
